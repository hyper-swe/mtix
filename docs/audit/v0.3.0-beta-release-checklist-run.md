# Release checklist run — v0.3.0-beta (2026-06-11)

Executed per docs/RELEASE-CHECKLIST.md. Tag target: the release-prep
commit once CI is green on it.

## 1. Deferred-finding review
- [x] All findings deferred since v0.2.0-beta listed: the four post-MTIX-15
      review items.
- [x] Each fixed or dispositioned — see
      `docs/audit/MTIX-22-deferred-dispositions.md` (VC-collision: doc fix
      applied; MaxConns: clean; harness drift: assert + comment applied;
      mem-ceiling: accepted with named trigger).
- [x] Dispositions recorded in docs/audit/.

## 2. Docs-coverage sweep
- [x] Commands/flags since v0.2.0-beta (`recover`, `import
      --recompute-checksum`) → USERMANUAL ok; auto-generated
      CLI_REFERENCE.md was stale, regenerated (MTIX-22.3).
- [x] MCP tools: 38 in code, 38 documented in MCP-SETUP.md; none added
      since v0.2.0-beta.
- [x] Env knobs (MTIX_MIN_FREE_BYTES, MTIX_SKIP_INTEGRITY_CHECK,
      MTIX_BACKUP_INTERVAL, MTIX_BACKUP_RETAIN) → USERMANUAL ok.
- [x] Exit codes 3/4 → USERMANUAL "Exit codes" ok.
- [x] CHANGELOG: [Unreleased] moved to [v0.3.0-beta] with headline and
      behavior-change notes; no schema migration (schema version
      unchanged).

## 3. Quality gates
- [x] `make preflight`: 7 passed, 0 failed. Two warnings, dispositioned:
      - Coverage 82.1% vs 85% threshold — pre-existing gap tracked by
        MTIX-2.4 (coverage-raising epic); not introduced by this release.
        New MTIX-26 code itself is test-first throughout.
      - "govulncheck not installed" — PATH artifact of the make target;
        govulncheck WAS run (see below): 0 reachable vulnerabilities.
- [x] `make verify` equivalents: race suite green, web tests 250/250,
      lint 0 issues, build ok.
- [x] Fault-injection conformance: green in CI on every recent push
      (runs 27344336235, 27351283568) and locally ×2 on a RAM disk.
- [x] Traceability gate green (runs inside the normal test job).
- [x] Security: govulncheck 0 reachable (after pinning toolchain
      go1.26.4 — fixes reachable net/textproto and crypto/x509 stdlib
      issues); npm audit 0 vulnerabilities (dev-dep advisories fixed);
      `make release-artifacts` regenerated.
      Branch security review (2026-06-11): no findings.
- [x] CI fully green on the exact tag commit (1686749, run 27352930212).

## 4. Tag and release
- [x] No hardcoded current-version strings to bump (version is
      ldflags-injected; remaining mentions are historical).
- [x] Tagged `v0.3.0-beta` on 1686749; no re-issue needed.
- [x] Release workflow green (run 27353448929): preflight, Supabase +
      Neon tag suites, goreleaser — 8 assets published.
- [x] Post-release smoke on the published darwin-arm64 artifact:
      version/commit correct (0.3.0-beta @ 1686749); init, create, list,
      recover (salvage report produced), automatic backup taken on first
      mutation, disk-full refusal exits 3 per contract.

## 5. Announce and follow up
- [x] Release notes call out behavior changes (free-disk floor,
      auto-backups default-on, exit codes) — included in CHANGELOG
      headline section consumed by the release.
- [ ] Notify the user whose 2026-05-19 data-loss incident drove this
      release that `mtix recover`, automatic backups, and the durability
      gates have shipped.
