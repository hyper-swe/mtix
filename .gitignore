# Binaries (root-level only, not cmd/mtix/ source directory)
/mtix
*.exe
*.dll
*.so
*.dylib

# Test artifacts
cover.out
coverage.out
*.out
*.test
*.prof

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Go
vendor/

# Marketing (internal planning — not public)
marketing/

# GoReleaser
dist/

# Release build artifacts (generated during CI release workflow)
BUILD-ENVIRONMENT.json
web-dist-checksums.txt
mtix-reference

# Node.js (root-level, e.g. from running npm in project root)
node_modules/

# Web build output
web/dist/
web/node_modules/
web/coverage/
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.js
web/vitest.config.d.ts

# Python
__pycache__/
*.pyc
*.egg-info/

# Database files (local dev)
*.db
*.db-wal
*.db-shm

# Backups
*.bak

# Claude artifacts
.claude/
*.tgz

# Playwright
.playwright-mcp/
screenshots/playwright/

# mtix local state (database, logs, generated docs — derived from .mtix/tasks.json)
.mtix/data/
.mtix/logs/
.mtix/docs/
.mtix/id_mapping.json
.mtix/tasks.json.tmp
# .mtix/config.yaml and .mtix/tasks.json ARE tracked (source of truth)

# Local test credentials (cloud-PG smoke-test DSNs — NEVER track)
.env.test.local
.mtix/secrets
.supabase-ca.crt

# mgit local store (never commit)
.mgit/

# mgit-sandbox generated scaffolding that can leak via squash patches
# (the real agent guide is generated from internal/docs/templates)
/.envrc
/AGENTS.md
.cursor/

# Local cloud-DB credentials for testing (never commit)
.env
.env.cloud
.env.local

# FR-19 preview build + local exec-trust records (never commit)
dist/mtix-fr19-preview
.mtix/hooks.trust
dist/mtix-fr19-preview-linux-arm64

# Internal design/incident artifacts — never publish (may name internal projects/agents)
internal-docs/
